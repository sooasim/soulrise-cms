{
    "name": "soulrise-netlify",
    "version": "1.0.0",
    "scripts": {
        "build": "parcel build index.html --public-url ./",
        "dev": "parcel index.html"
    },
    "devDependencies": {
        "parcel": "^2.0.0"
    }
}